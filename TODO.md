# TODO

## basic vs gpt

Change the exe name to basic and make it clear this is the analog of Karpathy's `basic.py` code, since GPT family tokenizers do not process the whole text body. 

There is room for improvement potentially on this code.

## gpt style

Use an extern regex library to split the text and then implement fast training on that.


